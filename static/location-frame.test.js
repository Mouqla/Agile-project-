test("I am so tired", () =>{
  console.log("End my suffering")
})
/*
const { createAndAppendFrame/*, clickPollutionInfo, createAndAppendDetailFrame,getPollutantDetails,prepareNextFrame,
closeFrame,  toggleCompare, openNav } = require('./location-frame');
const content = {
  city: "Sample City",
  country: "Sample Country",
  airQualityIndex: [50, "#00ff00"], 
  pollution: {
      PM25: 20, 
      PM10: 30,
      NO2: 10   
  },
  time: "2024-05-15 12:00:00", 
  forecast: [60] 
};

const location = {
  latitude: 40.7128,
  longitude: -74.0060,
  address: "Sample Address, Sample City, Sample Country",
  timezone: "UTC-5",
  elevation: 10,
  population: 1000000
};

describe('createAndAppendFrame', () => {
  beforeEach(() => {
      document.body.innerHTML = '<div id="offcanvas"></div>';
  });

  afterEach(() => {
      jest.clearAllMocks();
  });

  test('should create and append frame', async () => {
    // Mock fetch function to return a resolved promise with mock response
    global.fetch = jest.fn(() =>
        Promise.resolve({
            text: () => Promise.resolve('<div id="infoBox"></div>'), // Mock response HTML
        })
    );

    await createAndAppendFrame(content, location);

    expect(document.querySelector('.frame')).toBeTruthy(); // Check if frame is appended
  });

});

describe('clickPollutionInfo functions', () => {

});

describe('createAndAppendDetailFrame functions', () => {

});

describe('getPollutantDetails functions', () => {

});

describe('prepareNextFrame functions', () => {

});

describe('closeFrame functions', () => {

});

describe('toggleCompare functions', () => {

});

describe('openNav functions', () => {

});
*/